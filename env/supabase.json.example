{
  "_comment": "Copy to env/supabase.json (git-ignored) and run with --dart-define-from-file=env/supabase.json. The anon/publishable key is safe to embed; RLS protects the data. NEVER put the service_role/secret key here.",
  "SUPABASE_URL": "https://YOUR-PROJECT-ref.supabase.co",
  "SUPABASE_ANON_KEY": "your-anon-or-publishable-key"
}
